# Data — 5.6 GB of raw/generated material; regenerable via the collect/ + process/ scripts.
/data/
/OCR

# Training artifacts — checkpoints + TensorBoard logs, regenerable via model/train.py
runs/

# Python caches
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Python virtual environment
venv/

# Secrets
.env
OCR/.env
