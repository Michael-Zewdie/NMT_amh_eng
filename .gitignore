# Data — 5.6 GB of raw/generated material; regenerable via the collect/ + process/ scripts.
/data/
/OCR

# Archived bulk — the OCR tree carries its own .venv (12 GB) and archive/data is
# another 1.1 GB. The patterns above are root-anchored and do not reach these.
# archive/ code (collect/, processing/, experiments/) is still tracked.
archive/OCR/
archive/data/

# Training artifacts — checkpoints + TensorBoard logs, regenerable via model/train.py
runs/

# Python caches
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Python virtual environment
venv/
.venv/

# Secrets
.env
OCR/.env
