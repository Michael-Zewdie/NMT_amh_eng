# Core — everything needed to load a model and translate or evaluate.
# Installs cleanly on Linux/CUDA and on macOS (Intel or Apple Silicon).
#
#   python -m venv venv && . venv/bin/activate
#   pip install -r requirements.txt
#
# The data pipeline (collection, AfriCOMET/LID scoring, semantic clustering)
# needs the heavier set in requirements-pipeline.txt. You do not need it to run
# the trained models.
#
# torch is version-gated because 2.5.1 — what the models were trained on — has
# no wheels for Python 3.13. On 3.13 you get 2.6+, which loads these checkpoints
# fine. On Linux the default PyPI wheel is CUDA-enabled; to reproduce the exact
# training build use:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1 ; python_version < "3.13"
torch>=2.6 ; python_version >= "3.13"

tokenizers>=0.22
sacrebleu>=2.6
polars>=1.43
pandas>=2.2
numpy>=1.26
PyYAML>=6.0

# Training only — harmless elsewhere, and small.
tensorboard>=2.18
tqdm>=4.67
